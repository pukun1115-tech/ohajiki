const canvas = document.getElementById("canvas");
const ctx = canvas.getContext("2d");

canvas.width = 300;
canvas.height = 600;

const ohajikiArray = [];

function mainLoop() {
    ctx.crearRect(0, 0, canvas.width, canvas.height);

    requestA
}